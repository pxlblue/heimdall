version: '3'

services:
  heimdall:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APPNAME: heimdall
    volumes:
      - './config/local:/data/host:ro'
    env_file: ./config/local/.env
